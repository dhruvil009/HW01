language: python

python: 
  - 3.8

before_install:
  - pip install -U pip
  - echo "Testing"
  
install:
  - pip install -r requirements.txt
  
script:
  - echo "Hello World"
  
after_success:
  - echo "Test Success"
